<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.DayCounter.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.DayCounter</item>
    </style>

    <style name="Theme.DayCounter" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.DayCounter.WidgetPicker" parent="Theme.DayCounter">
        <item name="android:windowIsFloating">false</item>
    </style>
</resources>
